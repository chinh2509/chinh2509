#include <stdio.h>
int main(){
	int can[12],tongtb;
	for(int i=0;i<12;i++)
	{scanf("%d",&can[i]);
	
	}
		tongtb=(can[0]+can[1]+can[2]+can[3]+can[4]+can[5]+can[6]+can[7]+can[8]+can[9]+can[10]+can[11])/12;
	printf("tongtb=%d",tongtb);
	
	return 0;
	}
